server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;

  # Flutter routes are client-side paths. Static assets still win, while a
  # deep link falls back to the compiled index page.
  location / {
    try_files $uri $uri/ /index.html;
  }
}
